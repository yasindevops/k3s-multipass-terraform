# Local .terraform directories
**/.terraform/*

# .tfstate files (Hassas veri içerir, asla gönderme!)
*.tfstate
*.tfstate.*

# Crash log files
crash.log
*.log

# Generated files (Templatefile ile oluşan dosyayı da göndermiyoruz, çünkü her çalıştığında değişir)
worker_setup_rendered.yaml

# Exclude all .tfvars files, which are likely to contain sensitive data
*.tfvars

.terraform/
*.tfstate
*.tfstate.*
.terraform.lock.hcl
worker_setup_rendered.yaml